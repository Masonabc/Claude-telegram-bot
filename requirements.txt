requests>=2.28.0
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=12.0
lark-oapi>=1.4.0,<2.0

# Optional — voice message transcription (transcribe.py). Pulls in ctranslate2
# and downloads a model on first use. Without it, voice messages fail
# gracefully with a "couldn't catch that" reply instead of crashing.
faster-whisper>=1.0.0,<2.0
